name: mosaic
description: "Mosaic is a modular architecture for Flutter that enables clean separation of features using dynamic modules, internal events, UI injection, and centralized builds "
version: 0.0.3
homepage: "https://github.com/marcomit/mosaic"
repository: "https://github.com/marcomit/mosaic"

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  yaml_edit: ^2.2.2
  yaml: ^3.1.3
  http: ^1.4.0
  archive: ^4.0.7
  path: ^1.9.1
  # argv: ^1.0.3
  argv:
    path: ../argv

executables:
  mosaic: bin

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
