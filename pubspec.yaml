name: mosaic
description: A starting point for Dart libraries or applications.
version: 0.1.0
homepage: "https://pub.dev/packages/mosaic"
repository: "https://github.com/marcomit/mosaic"

environment:
  sdk: ^3.8.1

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  flutter:
    sdk: flutter
  yaml_edit: ^2.2.2
  # path: ^1.8.0

executables:
  mosaic: bin

dev_dependencies:
  lints: ^6.0.0
  test: ^1.24.0

optional-dependencies:
  flutter:
    sdk: flutter
